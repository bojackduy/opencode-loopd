{
  "name": "opencode-loopd",
  "version": "0.1.0",
  "description": "Codex-inspired goal engine for OpenCode with modal TUI dashboard.",
  "type": "module",
  "exports": {
    "./server": "./src/server/plugin.ts",
    "./tui": "./src/tui/plugin.tsx"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:server": "bun build src/server/plugin.ts --outfile dist/server.js --target bun --external @opencode-ai/plugin --external @opencode-ai/plugin/tool",
    "build:tui": "bun build src/tui/plugin.tsx --outfile dist/tui.js --target bun --external @opencode-ai/plugin/tui --external @opentui/core --external @opentui/solid --external solid-js",
    "build": "bun run build:server && bun run build:tui",
    "test": "bun test"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.4.0 <2"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.18.15",
    "@opentui/core": "latest",
    "@opentui/solid": "latest",
    "@types/node": "^26.2.0",
    "solid-js": "^1.9.13",
    "typescript": "^5.7.0"
  },
  "files": [
    "src"
  ],
  "license": "MIT"
}
